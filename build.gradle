group 'xq-java-gradle-custom-repository'
version '1.0-SNAPSHOT'

apply plugin: 'java'

sourceCompatibility = 1.8

repositories {
    maven {
        url 'http://localhost:5000/repository/maven-central/'
    }
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.12'
}
